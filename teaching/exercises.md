
Exercises for the `main_keywords.py` file:

    * Change the sleep: make it sleep 0.5 seconds and observe the output speed.

    * Add an elif range: print a special message for minutes 20–25.

    * Refactor math: write a square(n) function and use it inside a comprehension.

    * Count loops: keep a loops counter and stop after 10 iterations regardless of the time.

    * Challenge: replace the while loop with a for _ in range(30): loop that still allows Ctrl+C to exit.
