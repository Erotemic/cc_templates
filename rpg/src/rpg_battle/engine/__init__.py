"""Tiny engine-level helpers shared by scenes and rendering.

This package intentionally stays small. It holds low-level, reusable building
blocks like input key groups, tiny timing helpers, and scene protocol objects.
The actual battle rules live in ``core/`` and the classroom-facing game logic
lives in ``battle/``.
"""
